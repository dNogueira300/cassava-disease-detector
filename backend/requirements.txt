# Dependencias de producción — AmazoniaCrop backend
# Ubicación estándar (backend/requirements.txt) para que el builder de
# Railway/Docker las detecte. Copia sincronizada de src/requirements.txt.
#
# NOTA: torch/torchvision NO se listan aquí a propósito. El Dockerfile las
# instala en un paso previo desde el índice CPU-only de PyTorch (sin pin de
# versión, para evitar conflictos de resolución). Si se listaran aquí con
# versión fija, este `pip install -r requirements.txt` intentaría reinstalarlas
# desde el índice normal de PyPI (variante CUDA, mucho más pesada) y podría
# pisar la versión CPU ya instalada.
#
# Para recrear un entorno LOCAL desde cero, instala primero:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# y luego este archivo.
timm==1.0.15
numpy==2.2.5
Pillow==11.2.1
fastapi==0.115.12
uvicorn==0.34.2
python-multipart==0.0.20
requests==2.32.3
