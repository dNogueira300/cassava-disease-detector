# Desarrollo local. Con esta variable vacía o ausente, las llamadas usan rutas
# relativas y el proxy de Vite (vite.config.js) las redirige a localhost:8080.
# Se deja explícita para que apunte directo al backend local si se prefiere.
VITE_API_URL=http://localhost:8080
